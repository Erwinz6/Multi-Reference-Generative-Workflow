# Pipeline Overview

This repository follows the thesis methodology as a chained process with three connected stages:

1. batik-aware lightweight generation,
2. role-separated multi-reference transfer,
3. protocol-based 2D-to-3D material updating.

The public GitHub release keeps the computational structure of the paper while replacing the original Unreal Engine project assets with a minimal public-safe reproduction path.
